package hapi;

/**
 * Enumeration of the supported HAPI data stream formats.
 * 
 * @author
 */
public enum DataFormat
{
	/** Format corresponding to binary stream. */
	BIN,

	/** Format corresponding to a (text) comma separated value stream. */
	CSV,

	/** Format corresponding to a (text) JSON stream. */
	JSON,

}
